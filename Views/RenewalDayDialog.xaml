<Window x:Class="CursorQuotaProgress.Views.RenewalDayDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Set Renewal Day"
        Height="250" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" TextWrapping="Wrap" Margin="0,0,0,15">
            <Run Text="Select your Cursor quota renewal day (1-31)."/>
            <LineBreak/>
            <Run Text="Months without this day will be skipped."/>
        </TextBlock>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Renewal Day:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="RenewalDayTextBox" Width="60" VerticalAlignment="Center"
                     Text="15" MaxLength="2"/>
        </StackPanel>

        <TextBlock x:Name="PreviewText" Grid.Row="2" TextWrapping="Wrap"
                   Foreground="Gray" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="75" Margin="0,0,10,0"
                    Click="OnOkClick" IsDefault="True"/>
            <Button Content="Cancel" Width="75"
                    Click="OnCancelClick" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
